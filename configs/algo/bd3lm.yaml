name: bd3lm
backbone: dit # dit / dimamba / hf_dit
parameterization: subs
time_conditioning: False
# 🔄 CHANGED: T=1000 (discrete diffusion) for fair comparison with HDP
# - Training: Sample timestep t from {0, 1, ..., T-1}
# - Inference: Use num_sampling_steps (e.g., 100-500 steps)
T: 1000 # 1000 (discrete timesteps) for fair HDP comparison
causal_attention: False
dropout: 0.0
ignore_bos: True
cross_attn: True
var_min: True
clip_search_delta: 0.05
clip_search_widths: []
fix_clipping: False
sampler: semi_ar
mdlm_loss_scale: False
